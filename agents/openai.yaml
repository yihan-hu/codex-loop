interface:
  display_name: "Codex Loop"
  short_description: "Objective lifecycle for multi-step work"
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
  default_prompt: "Use $codex-loop to carry this objective end to end, prewarming predictable external permissions early and composing with the relevant domain workflow and host tools."
policy:
  allow_implicit_invocation: true
