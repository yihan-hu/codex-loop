interface:
  display_name: "Codex Loop"
  short_description: "Objective lifecycle for multi-step work"
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
  default_prompt: "Use $codex-loop for this objective; before any task action, bootstrap a new lifecycle or reuse the exact task_id for an already-admitted continuation, and never continue outside that lifecycle."
policy:
  allow_implicit_invocation: true
