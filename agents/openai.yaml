interface:
  display_name: "Codex Loop"
  short_description: "Adaptive lifecycle for multi-step objectives"
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
  default_prompt: "Use $codex-loop to carry this objective end to end, adapting between direct and durable execution and composing with the relevant domain workflow and host tools."
policy:
  allow_implicit_invocation: true
