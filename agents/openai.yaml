interface:
  display_name: "Codex Loop"
  short_description: "Objective lifecycle for multi-step work"
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
  default_prompt: "Use $codex-loop to carry this objective end to end, preserve the user-authorized scope across continuation, and resolve external permissions only at the side-effect boundary that needs them."
policy:
  allow_implicit_invocation: true
