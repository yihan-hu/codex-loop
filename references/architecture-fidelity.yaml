{
  "schema_version": 1,
  "observed_upstream": {
    "repository": "openai/codex",
    "commit": "2b7c279735d0d096cf7b34fe98938f46792f4d4f",
    "observed_date": "2026-09-01",
    "note": "Architecture observation pin; independent from the frozen exact-resource maintenance baseline in source-map.yaml."
  },
  "review_statuses": [
    "ALIGNED",
    "PARTIAL",
    "HOST_GAP",
    "LOCAL_DIVERGENCE",
    "NOT_APPLICABLE",
    "NEEDS_REVIEW"
  ],
  "watch_surfaces": [
    {
      "id": "workspace_members",
      "layer": "source",
      "paths": [
        "codex-rs/Cargo.toml"
      ],
      "required_patterns": [
        "ext/goal",
        "ext/queue",
        "ext/extension-api",
        "agent-graph-store"
      ],
      "status": "ALIGNED"
    },
    {
      "id": "extension_lifecycle",
      "layer": "control_plane",
      "paths": [
        "codex-rs/ext/extension-api/src/contributors.rs"
      ],
      "required_patterns": [
        "ThreadLifecycleContributor",
        "TurnLifecycleContributor",
        "ToolLifecycleContributor",
        "TokenUsageContributor"
      ],
      "status": "NOT_APPLICABLE",
      "divergence": "Codex Loop intentionally no longer mirrors a thread/turn lifecycle contributor graph for ordinary task execution; the host model owns the agent loop.",
      "upgrade_path": "None unless a concrete host-side lifecycle need appears."
    },
    {
      "id": "execution_lifetime_separation",
      "layer": "control_plane",
      "paths": [
        "codex-rs/core/src/exec.rs"
      ],
      "required_patterns": [
        "ExecExpiration",
        "kill_child_process_group",
        "IO_DRAIN_TIMEOUT_MS"
      ],
      "status": "PARTIAL",
      "divergence": "Codex Loop now separates workload, process, and cleanup outcomes and reuses owned process-group cleanup where its local supervisor can observe it, but ChatGPT host-visible commands do not universally expose Codex's native exec/process-group lifetime primitives.",
      "upgrade_path": "Bind ExecutionObservation directly to native host exec lifecycle metadata/process ownership when exposed; keep workload terminal evidence independent from process termination and remove host-gap adapters rather than adding framework-specific forced-exit wrappers."
    },
    {
      "id": "app_server_events",
      "layer": "control_plane",
      "paths": [
        "codex-rs/app-server-protocol/src/protocol/common.rs"
      ],
      "required_patterns": [
        "turn/started",
        "turn/completed",
        "item/started",
        "item/completed"
      ],
      "status": "PARTIAL",
      "divergence": "The Skill cannot own an independent app-server notification stream or UI spinner.",
      "upgrade_path": "Keep progress as a projection layer; bind it to native host events if exposed."
    },
    {
      "id": "goal_and_idle_dispatch",
      "layer": "concept",
      "paths": [
        "codex-rs/ext/goal/src/extension.rs",
        "codex-rs/ext/queue/src/service.rs"
      ],
      "required_patterns": [
        "ThreadLifecycleContributor",
        "ThreadIdleInput"
      ],
      "status": "HOST_GAP",
      "divergence": "The current Web Skill surface has no start_turn_if_idle primitive for automatic continuation.",
      "upgrade_path": "Keep durable objective/manual resume semantics behind a continuation adapter that can swap to automatic idle dispatch later."
    },
    {
      "id": "token_and_rollout_budget",
      "layer": "concept",
      "paths": [
        "codex-rs/features/src/feature_configs.rs"
      ],
      "required_patterns": [
        "TokenBudgetConfigToml",
        "RolloutBudgetConfigToml",
        "auto_compact_fallback_prompt"
      ],
      "status": "HOST_GAP",
      "divergence": "Exact remaining context/token budget is not exposed to the Skill runtime.",
      "upgrade_path": "Use host budget metadata when available; otherwise only heuristic precautionary checkpoints and never invent token counts."
    },
    {
      "id": "turn_suspension",
      "layer": "concept",
      "paths": [
        "codex-rs/core/src/session/turn_suspension.rs"
      ],
      "required_patterns": [
        "suspend_turn_and_shutdown",
        "flush"
      ],
      "status": "PARTIAL",
      "divergence": "Codex Loop has checkpoint/re-entry but cannot transfer ownership between Codex workers.",
      "upgrade_path": "Preserve flush-before-stop, external-action reconciliation and safe resume semantics without claiming host suspension authority."
    },
    {
      "id": "history_notes",
      "layer": "concept",
      "paths": [
        "codex-rs/ext/history-notes/src/tools.rs"
      ],
      "required_patterns": [
        "history",
        "notes",
        "list_windows",
        "read_item"
      ],
      "status": "PARTIAL",
      "divergence": "No Codex backend history/notes service exists in ChatGPT Skill runtime.",
      "upgrade_path": "Use optional schema-whitelisted persistence and evidence drill-down; prefer native history APIs if exposed."
    },
    {
      "id": "retained_user_request",
      "layer": "control_plane",
      "paths": [
        "codex-rs/protocol/src/protocol.rs"
      ],
      "required_patterns": [
        "USER_MESSAGE_BEGIN"
      ],
      "status": "PARTIAL",
      "divergence": "The ChatGPT Skill runtime does not expose Codex conversation-item identity/history as a local authority primitive. Codex Loop keeps the host conversation as authority for every selected lifecycle, uses stateless orient to echo the exact request anchor into host-visible execution context before mutation, and persists request/steers only when durable state is enabled.",
      "upgrade_path": "Bind request authority directly to native immutable host user-message/item identity when exposed; keep summaries and criteria as derived execution aids rather than task truth."
    },
    {
      "id": "single_in_progress_focus",
      "layer": "control_plane",
      "paths": [
        "codex-rs/protocol/src/plan_tool.rs"
      ],
      "required_patterns": [
        "InProgress"
      ],
      "status": "ALIGNED",
      "divergence": "Codex Loop now uses a thin Codex-style plan with pending/in_progress/completed statuses and at most one in_progress step; it is execution memory rather than a semantic gate.",
      "upgrade_path": "Use a native host update_plan primitive directly when exposed; otherwise keep this local representation thin."
    },
    {
      "id": "coding_precision_policy",
      "layer": "concept",
      "paths": [
        "codex-rs/protocol/src/prompts/base_instructions/default.md"
      ],
      "required_patterns": [
        "Do not attempt to fix unrelated bugs or broken tests",
        "start as specific as possible"
      ],
      "status": "ALIGNED"
    },
    {
      "id": "apply_patch_boundary",
      "layer": "control_plane",
      "paths": [
        "codex-rs/apply-patch/src/parser.rs"
      ],
      "required_patterns": [
        "*** Begin Patch",
        "*** Update File: "
      ],
      "status": "PARTIAL",
      "divergence": "Codex Loop adopts patch-first model behavior but keeps patch grammar parsing, filesystem application, sandboxing, and tool dispatch with the active host rather than duplicating Codex's native patch engine inside the portable runtime.",
      "upgrade_path": "Prefer a native host apply-patch primitive. Only port patch execution locally if the host cannot provide it and the full filesystem/sandbox safety contract can be preserved without creating a competing mutation engine."
    },
    {
      "id": "agent_graph_store",
      "layer": "concept",
      "paths": [
        "codex-rs/agent-graph-store/src/lib.rs"
      ],
      "required_patterns": [
        "AgentGraphStore"
      ],
      "status": "LOCAL_DIVERGENCE",
      "divergence": "Current logical isolation is serialized local fallback, not native Codex Multi-Agent V2.",
      "upgrade_path": "Keep agent topology semantics separable from execution and route to native host subagents when available."
    },
    {
      "id": "multi_agent_v2",
      "layer": "concept",
      "paths": [
        "codex-rs/features/src/feature_configs.rs"
      ],
      "required_patterns": [
        "MultiAgentV2ConfigToml"
      ],
      "status": "LOCAL_DIVERGENCE",
      "divergence": "Current logical isolation is serialized local fallback, not native Codex Multi-Agent V2.",
      "upgrade_path": "Keep agent topology semantics separable from execution and route to native host subagents when available."
    },
    {
      "id": "context_fragments",
      "layer": "control_plane",
      "paths": [
        "codex-rs/context-fragments/src/fragment.rs"
      ],
      "required_patterns": [
        "ContextualUserFragment",
        "content_kind",
        "markers"
      ],
      "status": "PARTIAL",
      "divergence": "Codex Loop uses bounded context projections without the full upstream fragment ownership/type system.",
      "upgrade_path": "Add owner/scope/freshness/replace semantics only when multiple injected projections require them."
    },
    {
      "id": "conversation_routing_plane",
      "layer": "control_plane",
      "paths": [],
      "required_patterns": [],
      "status": "LOCAL_DIVERGENCE",
      "divergence": "The ChatGPT Skill host does not expose a native conversation-scoped workspace/interaction/deployment routing register or a tool-dispatch interceptor. Codex Loop therefore materializes routing in a private temp JSON file and requires deterministic route-check preflights before host actions.",
      "upgrade_path": "Replace the local routing file/adapter with native host routing metadata or tool-dispatch policy when exposed, while preserving the independent workspace_mode, interaction_target, and deployment_target semantics and fail-closed local transitions."
    },
    {
      "id": "manual_skill_installation_boundary",
      "layer": "control_plane",
      "paths": [],
      "required_patterns": [],
      "status": "HOST_GAP",
      "divergence": "Codex Loop distribution produces one official validated skill.zip, then exposes those exact bytes under the chat-download filename codex-loop.zip. The rename is byte-preserving and exists only to keep the package on the normal download surface; installing or replacing it is a user-owned action and is not represented as Codex Loop lifecycle state.",
      "upgrade_path": "Keep official Skill Creator packaging, byte-identical chat-download naming, and host installation as separate boundaries. Revisit the delivery filename only through an explicit design change if the host download/install routing behavior materially changes."
    },
    {
      "id": "optional_cloud_persistence",
      "layer": "concept",
      "paths": [],
      "required_patterns": [],
      "status": "LOCAL_DIVERGENCE",
      "divergence": "Google Drive persistence is a ChatGPT Web reliability adapter, not an upstream Codex storage implementation.",
      "upgrade_path": "Keep a backend-neutral persistence interface so a future native host durable store can replace Drive without changing task semantics."
    },
    {
      "id": "workspace_capsule_persistence",
      "layer": "concept",
      "paths": [],
      "required_patterns": [],
      "status": "LOCAL_DIVERGENCE",
      "divergence": "Drive-backed Workspace Capsules preserve exact Web Git commit/tree plus staged, unstaged, and non-ignored untracked state across ephemeral conversation workspaces. This is a ChatGPT host recovery adapter, not an upstream Codex persistence implementation.",
      "upgrade_path": "Prefer a native host workspace snapshot/restore primitive if one becomes available while retaining one-shot consumption, 3-day bounded retention, exact Git/state verification, and cleanup-failure independence."
    },
    {
      "id": "git_like_repository_continuity",
      "layer": "control_plane",
      "paths": [],
      "required_patterns": [],
      "status": "LOCAL_DIVERGENCE",
      "divergence": "ChatGPT Web may lose a temporary repository path between turns/conversations, so Codex Loop cannot rely on a continuously mounted Git object database exactly like a native local Git client. The runtime therefore separates path-independent source provenance from a path-bound workspace lease and routes repository entry HOT -> WARM -> COLD.",
      "upgrade_path": "If the host exposes durable native Git workspaces, map HOT leases directly to that primitive and retain WARM restore only for disaster recovery. Preserve the invariant that remote movement is incremental synchronization state and that cold source acquisition is never the normal push path."
    }
  ]
}
