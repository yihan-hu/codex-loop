# Attribution

This Skill is an independent compatibility layer derived from the public OpenAI Codex repository. It is not the Codex CLI, does not include the Codex model runtime, and does not replace the ChatGPT host runtime.

Upstream source: `openai/codex`.

The bundled `LICENSE` and `NOTICE` are exact copies from the audited upstream revision recorded in `references/source-map.yaml`. Other bundled upstream resources are tracked in `scripts/upstream/MANIFEST.json`. Cross-language Python implementations are behavioral/local-runtime ports and are identified as such in the source map; they are not represented as byte-identical OpenAI source files.
